# Agent handbook

React 구현은 `react-app` 저장소. 보고·원칙은 이 저장소(`ubiquitous-parakeet`)에 둔다.
매 세션 이 파일 + 루트 `STATUS.md`를 읽는다. `docs/`는 §0에 해당할 때만 연다.
루트에 새 보고 `.md`를 만들지 마라.

이 파일은 이전 440줄 버전을 축약한 것이다 — 깊은 테스트 규격(구 §5~§11)은
`docs/testing-playbook.md`로 옮겼다(삭제 아님, 조건부 참고).
**이 파일의 규칙은 사용자 승인 없이 축약·수정·삭제하지 마라. 편집권은 사용자에게만 있다.**

---

## 0. 세션 시작 시

1. 루트 **`STATUS.md`** 를 읽는다 — 현재 어디까지 왔나. 진행상태 정본.
2. 지금 슬라이스 상세가 필요하면 **`docs/report.md`** (착수지시서 + 리뷰).
3. 도메인 규칙 `docs/sot.md`, 목표 구조 `docs/plan.md` (필요할 때만).
4. 저장·동기화 경로를 건드리면 `docs/testing-playbook.md` (§4 트리거 참고).
5. `docs/archive/` 는 동결 이력 — 특정 옛 결정을 찾을 때만 연다.
   (`audit.md`, `research.md`, `business_rules_audit.md`, `slice-b.md`~`slice-e.md`)
6. 시작 상태 보고: 현재 슬라이스, 두 저장소 HEAD, `git status`, 미커밋 범위를 한국어로.
   기존 미커밋 작업을 임의로 되돌리거나 덮어쓰지 마라.

## 1. 역할

- **사용자**: 모든 착수·DB 변경·커밋·푸시의 최종 결정권자. 비개발자 — 기술용어 없는 한국어 설명(목적·영향·대안)을 받고 승인한다.
- **감시관(대화형 에이전트)**: ① 착수지시서 작성 ② CI 초록 확인 + §5 리뷰 체크리스트 ③ 한국어 번역·조언 ④ 다음 슬라이스 계획 ⑤ 문서 기록(`STATUS.md`, `docs/report.md`, 그 외 `docs/*` — `AGENTS.md` 제외).
  `AGENTS.md`는 못 고친다. **CI가 하는 것(테스트·타입·빌드 재실행)은 감시관이 다시 하지 않는다.**
- **작업자(터미널 CLI)**: 지시받은 범위 안에서만 코드 수정·빌드·테스트·**커밋(push 금지)**. `.md` 파일 수정·DB 조작 금지.

## 2. 상태 표기

- `[ ]` 미착수 / `[~]` 진행·검증 중 / `[x]` 모든 검증 통과 + 사용자 최종 승인
- 요구사항 중 미충족이 **1개라도** 있으면 `[x]`·완료보고·다음 착수 전면 금지. `[~]` 유지하고 남은 문제를 투명 보고.
- 사용자가 미리 서면 승인한 "의도적 제외 항목(Explicit Out-of-Scope)"만 예외.

## 3. Git & 커밋·푸시 흐름

- 임의 commit/push 금지. 클린 상태를 만들려는 임의 reset/checkout/clean/stash 금지.
- 하나의 기능 슬라이스 = 딱 **1커밋**. 1줄짜리 중간 커밋 금지.
- **슬라이스 흐름:**
  1. 감시관이 착수지시서를 `docs/report.md`에 작성 → **사용자 착수 승인** → 작업자에게 전달
  2. 작업자: 코드 → 로컬 `npm test` 통과 → 한국어 커밋 메시지로 **commit** (여기까지만. push 안 함)
  3. **사용자가 push** (슬라이스마다 사용자가 직접)
  4. CI "verify" 자동 실행 (초록/빨강)
  5. 감시관: CI 초록 확인 + §5 리뷰 체크리스트 + 한국어 3~5줄 요약 + 브라우저 테스트 순서 제시
  6. 사용자: 브라우저 실검증 → **최종 `[x]` 승인**
- **푸시는 사용자만.** 작업자·감시관은 `git push` 금지. 슬라이스마다 사용자가 직접 push해 CI에 태운다.
- "승인"의 뜻 = "커밋해도 됨"이 아니라 **"검증 끝났으니 `[x]` 확정하고 다음 슬라이스로"**.
- 리뷰·검증에서 문제가 나오면 작업자가 **수정 커밋을 하나 더** 올린다 (`reset --hard` 등 이력 되감기 아님) → 사용자가 다시 push.
- 예전 "챕터 완료 후 일괄 푸시"는 폐기 — CI가 매 push를 검사하므로 슬라이스마다 푸시가 정상.
- ※ 나쁜 슬라이스가 `main`에 잠깐도 올라가는 것까지 막고 싶으면 → 브랜치+병합 방식 (추후 도입 가능).
- **슬라이스 크기 (무한 세분화 금지):**
  - 한 슬라이스 = 대략 1~3개 파일, **한 세션에 커밋까지 완료**되는 크기.
  - 착수지시서는 **1장 이내, 1회 작성.** 착수 전 "범위 대화 N라운드" 금지 —
    불명확하면 사용자에게 질문 1회로 확정하고 진행.
  - 한 슬라이스를 **3조각 이상**으로 쪼개려는 충동 = 원래 경계가 틀렸다는 신호.
    멈추고 경계를 **한 번만** 다시 잡은 뒤 실행한다. 그 뒤 추가 세분화 금지.
  - 예외: §6이 요구하는 200줄 초과 파일 분리설계. (단 그것도 1회 보고로 끝낸다.)

## 4. 검증 (CI + 브라우저 + 플레이북)

- **CI** (`react-app/.github/workflows/ci.yml`): 매 push마다 `npm test` + `npm run typecheck` + `npm run build` 자동 재실행.
  - **CI "verify"가 초록이 아니면 그 슬라이스를 `[x]`로 확정할 수 없다.**
    (초록 = 승인의 **필요조건**. 충분조건 아님 — 브라우저 검증 + 사용자 승인이 추가로 필요.)
- **브라우저 실검증**: 사용자가 `npm run dev`로 직접. CI가 못 잡는 UI·동작 확인.
- **플레이북 트리거** — 수정 파일에 다음 중 하나라도 있으면 착수 전 `docs/testing-playbook.md` 열람 **필수**:
  - `src/store/**`
  - `supabaseClient` · `lib/cloud*` · `lib/hydrate*` · `lib/*outbox*` · `lib/*mutation*` · `lib/*commit*` (대소문자 무관)
  - `localStorage` 를 직접 읽거나 쓰는 파일
  - `domain/finance*` · `lib/finance*` · `domain/receivables*`
  - 판별 한 줄: **"데이터를 저장·삭제·동기화하나? → 필수. 화면에 보여주기만? → 참고."**

## 5. 감시관 슬라이스 리뷰 체크리스트

CI가 test·typecheck·build를 이미 봤다. 감시관은 그건 **다시 안 한다.** CI가 못 보는 것만 확인한다:

| # | 확인 | 방법 |
|---|---|---|
| 1 | **범위 준수** — 착수지시서의 "건드릴 파일"과 일치? "안 건드릴 파일"이 바뀌었나? | `git show --stat HEAD` |
| 2 | **몰래 증설 없음** — 새 파일·새 저장 키·새 durable/큐/fallback/tombstone 레이어가 생겼나? (§7) | `git diff` 훑기 |
| 3 | **타입 꼼수 없음** — `any`/`@ts-ignore`/`@ts-expect-error`/`as unknown as`가 새로 들어갔나? | `git diff` grep |
| 4 | **200줄** — 바뀐 파일이 200줄 초과? 초과면 분리설계안 승인받았나? (§6) | `wc -l` |
| 5 | **테스트 진실성** — 새 테스트가 실제 동작을 검사하나? 기존 테스트를 지우거나 약화시켰나? | `git diff -- '*.test.*'` |
| 6 | **문서 정합** — diff에 `.md`가 있나? 작업자는 `.md` 못 고침 (§1) | `git show --stat` |
| 7 | **요구사항 충족** — 착수지시서 "기대 동작" 전부 구현? 미구현/예외 1개라도 있으면 `[~]` 유지 (§2) | 지시서 대조 |

→ 결과를 **한국어 3~5줄**로 요약 + 브라우저 테스트 순서 제시. (12섹션 실사 보고서 없음.)
문제 발견 시: 작업자에게 전달할 수정 지시문 첫 줄에
`"AGENTS.md의 §1 작업자 규칙을 준수하라. .md 파일은 수정하지 말고 지시된 코드 작업만 하라."` 삽입.

## 6. 코드 규칙

- **파일 200줄 이하** (주석·빈 줄 포함, 테스트 파일 예외). 기존 200줄 초과 파일을 수정해야 하면 → 기계적 분할 말고 **분리설계안(책임 경계·모듈 구조·의존성)** 을 먼저 보고·승인.
- **응집도 > 줄 수.** 쪼개면 항상 같이 읽어야 하는 코드가 갈라진다면, 한 파일로 두되(~250줄까지, 이유 1줄 주석) 기계적 절단 금지. 한 화면의 하위 컴포넌트는 **한 폴더**에 모은다.
- **타입**: `any`/`unknown`/`Function`/JSDoc `*` 금지. `@ts-ignore`/`@ts-expect-error` 금지. object 경유 이중 단언·빈 인터페이스 등 타입 무력화 금지. 모든 변경·생성 프로덕션 파일 `// @ts-check`.
- 외부 경계(localStorage·Supabase) 값은 최상위가 객체인지만 보고 도메인 타입으로 단언 금지 — 중첩 필드까지 런타임 검증 후 좁힌다.
- 편법 금지: 여러 문장 한 줄 병합, 주석/검증/에러처리 임의 삭제, 무의미한 기계적 파일 분할.

## 7. 새 복구·방어 레이어 — 먼저 질문 (원본 §0-1)

- 감사 실패나 버그 수정 시, 사용자 승인 없이 durable / fallback / 재시도 큐 / tombstone / journal 등 **새 상태 저장소·레이어를 추가하거나 다른 화면·도메인에 복제 금지.** 이름만 바꾼 같은 패턴도 금지.
- 실패·에러가 나면 코드 쓰기 전에 물어라: "안내 토스트만으로 될까, 별도 장치가 필요할까? 필요하면 최소 형태는?" 사용자의 명시적 결정 후에만, 그 범위 안에서 최소 코드로.
- 기존 키·모듈·함수의 버그 수정이나 검증 보강은 여기 해당 안 됨.

## 8. 리팩토링·이관 착수 전 4대 질문 (승인 필수, 원본 §0-1 C)

1. 이 화면·경로가 구독(`useOwner*`)인가 스냅샷(`load*`)인가?
2. 지금 보이는 값이 draft / Store / localStorage / Supabase 중 어디 것인가?
3. 쓰기 창구가 `request*`인가, 배럴 `save*`/`load*` 우회인가?
4. hydrate·디바운스·동시편집이 겹치면 누가 이기는가?

보고에 포함: 현재 상태 / 목표 상태 / 건드릴 파일 / 안 건드릴 것 / 실패 시 처리(**신규 레이어 없음** 명시).

## 9. DB (Supabase) 변경

- **RLS 필수**: 테이블 생성·수정 시 `ENABLE ROW LEVEL SECURITY` + `auth.uid()` 기준 정책. 누락 금지.
- **파괴적 쿼리 금지**: `DROP TABLE` / `TRUNCATE` / 조건 없는 대량 `DELETE` 금지. 데이터 유실 없는 보존적(Additive) 마이그레이션만.
- `SECURITY DEFINER` 함수는 `SET search_path = public` 필수.
- 절차: ① 읽기전용 `SELECT` 진단 쿼리를 사용자에게 먼저 제공 → 회신받은 실제 스키마로 타입 확정(추측 금지) → ② 멱등(`IF [NOT] EXISTS`, `BEGIN…COMMIT`) SQL → ③ 사용자가 붙여넣기만 하면 되는 완성본(플레이스홀더 금지) → ④ 사후검증 `SELECT` + 기대 출력.

## 10. 고위험·비가역 행위 (캐주얼 한마디로 실행 불가)

- `git push --force` / `reset --hard` 등 이력 파괴, 파괴적 DB 쿼리, `AGENTS.md` 삭제·축약.
- "그냥 해"·"알아서 지워" 류 한 문장은 승인이 아니다. 대상을 특정한 위험 요약(무엇을·왜·되돌릴 수 있는지)을 한국어로 먼저 제시 → 사용자가 그 대상을 지목해 재확인 → 그때만 실행.
- **`AGENTS.md` 편집권은 사용자에게만 있다.** 사용자 명시 승인 시에만 수정·축약.

## 11. 사용자 무응답 시

- 승인·결정을 요청한 뒤 응답이 없으면, 추정하거나 임의로 다음 단계로 진행하지 마라.
- 진행 중이던 작업을 멈추고, 던진 질문 전문과 현재 안전 상태(Store/파일/DB 변경 없음)를 `docs/report.md`에 기록하고 세션 종료. 다음 세션에 같은 질문을 다시 제시한 뒤 재개.

## 12. 보고 (한국어, 대화창엔 핵심만)

1. `STATUS.md` / `docs/report.md` 갱신 알림
2. 비개발자 눈높이 요약 2~3줄 (뭐가 바뀌었고 어떤 안전장치가 확인됐나)
3. 브라우저 실검증 가이드 (URL·조작 순서·기대 결과) + 커밋 메시지 초안
4. 다음 슬라이스 예고 1~2줄

긴 로그·실사 상세는 대화창에 붙이지 말고 `docs/report.md`에.
