# docs/report.md — ④ 착수 전 (매출 탭 수치 불일치 조사)

> 슬라이스마다 리셋되는 착수지시서·실사 통합 파일(AGENTS §12).
> 이관 계획 ①·②(①~④)·③(③-1~③-4+분리설계)는 전부 `[x]` 확정됨 — 상세는
> `STATUS.md` "완료" 절 참고.

## 이관 계획 ③ 전체 완료 (2026-09-08)

"월간 운송료 정산" 위젯 구조 통일 — ③-1(공용 계산 함수 `monthSettlementSummary`)·
③-2(캘린더 카드 연결)·③-3(리포트 요약 연결)·③-4(리포트 일자별 표 이관)·
분리설계(§6 200줄 준수, `ReportSummaryContent.jsx`/`reportSummary.js`/
`reportDetail.js` 분리) 전부 CI 초록·감시관 §5 통과·보리 명시 승인("승인")
받고 `[x]`. 상세는 `STATUS.md` "완료" 절.

## ④ 착수 전 — 아직 조사·착수지시서 작성 안 함

`STATUS.md` "1차 조사에서 발견한 것" 절 요약:

> 매출 화면 "전체 손익"·"기사" 탭 순이익이 원본과 react-app에서 부호까지
> 반대로 나옴. 거래처 목록에 react-app에만 있는 "연습운수", 미수금도 원본
> 0건 vs react-app 2건 — 셋 다 서브차량(22나2222) 관련 데이터 처리 차이로
> 추정.

원본 상세는 `docs/archive/audit.md` "원본↔React UI 전수 대조 — 1차 조사"
절 §4·§6-1·§6-2 참고(동결 이력, 필요할 때만 열람).

## 다음 세션 시작 시 할 일

1. `docs/archive/audit.md` §4·§6-1·§6-2 재확인 → 원본 코드(`ubiquitous-parakeet`)와
   react-app 해당 화면 직접 대조해 원인 좁히기.
2. 서브차량(22나2222) 데이터 처리가 실제 원인인지 확인.
3. 조사 결과로 착수지시서 작성 → 보리 착수 승인 → 작업자 전달.
