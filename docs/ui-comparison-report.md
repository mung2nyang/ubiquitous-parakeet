# UI 비교 보고서 — 원본 vs react-app (작성 중)

> 코드 수정 없음. 조사·기록만. 보리 지시(2026-09-08): 기사 계정은 제외(이관본은
> 차주/기사 화면 구분이 거의 없음), 게스트·차주만 비교. 발견사항 전부 비개발자
> 언어로 정리 후 조율 → 승인되면 작업 계획 수립.
>
> **⚠️ 2026-09-09 보리 확인: §2~13(마이페이지~고객센터, "자동 비교 초안")은
> 내용이 다 틀렸다 — 폐기, 참고·재사용 금지.** §1(홈 캘린더)·§1-1(사이드메뉴)만
> 보리가 실제로 화면 짚어가며 검토·확정한 유효한 내용이다. §2~13을 다시 쓰려면
> 처음부터 같은 방식(보리가 직접 화면 확인)으로 다시 해야 한다.

## 방법

- 두 앱을 같은 크기(모바일 375×812)로 띄워 화면별 스크린샷 대조.
- 라이트/다크 테마 각각.
- 계정: 차주(현재 로그인된 테스트 계정, "테스트님") → 게스트("비회원으로 시작").
- "잘림/겹침처럼 보이는 것"은 스크린샷만으로 단정하지 않고 `document.body.scrollWidth`
  등 코드로 교차검증 후 기록(브라우저 도구 자체의 캡처 확대 때문에 실제로는
  문제없는 경우가 있었음 — 방법 검증 과정에서 확인).
- 심각도: 🔴 높음(기능 손실·오작동) / 🟡 중간(눈에 띄는 시각 차이) / ⚪ 낮음(사소)

## 요약 (차주·라이트·모바일 1차 결과)

🔴 높음 3건 · 🟡 중간 13건 · ⚪ 낮음 9건 · ✅ 완전 일치 화면 3개(일일운행·
운송비내역서 본문·고객센터)

**🔴 높음 3건**:
1. 마이페이지에 "초대코드로 차주 연동" 메뉴가 차주 계정에도 뜸(코드 확인,
   §2)
2. 운송비 내역서 — 서브차량(기사) 리포트를 볼 차량 선택 모달 자체가 없음(§10)
3. 기사연동관리 — 안내 문구·입력 폼 노출 방식이 원본과 크게 다름(§12)

## 발견 사항 (차주, 라이트, 모바일)

### 1. 홈(캘린더) — 보리 직접 검토·확정 (2026-09-08)

> 아래 6개는 보리가 직접 화면 캡처로 짚어주고, 감시관이 코드로 원인까지
> 확인한 뒤 확정한 것. 최초 자동 비교로 나왔던 "햄버거 메뉴 버튼 배경 차이"
> 항목은 실제로 동일해서(원본도 투명 배경) 삭제.

1. 🟡 **"년"/"월" 옆 아래 화살표(▼)** — react-app에만 있음("2026년⌄ 9월⌄"),
   원본은 순수 텍스트만("2026년 9월").
2. 🟡 **상단 헤더가 원본보다 위로 붙어있음** — 벨·로고·햄버거 메뉴, 날짜
   선택 알약까지 포함한 헤더 블록 전체가 react-app이 원본보다 위쪽에 바짝
   붙어있음(원본 대비 날짜 알약 시작 위치가 약 50px 정도 차이). 전체적으로
   아래로 내려야 함.
3. ✅ **벨/햄버거 아이콘 원형 배경 — 확인 결과 문제 아님.** 원본도 투명
   배경이라 육안으로는 안 보임, react-app도 마찬가지 — 실질적으로 동일.
   손대지 않음(보리 확인).
4. 🟡 **날짜 칸 클릭 시 하이라이트가 다크모드에서 흰색으로 뜸** — 원본은
   눌렀을 때 보기 편한 회색인데 react-app은 흰색이 튐. **원인 확정**:
   다크모드 배경색 변수(`--hover-bg`/`--today-bg`)가 [account-flow.css:25-36](react-app/src/account-flow.css:25)
   에서만 어두운 값으로 재정의되는데, [main-calendar.css:1-14](react-app/src/main-calendar.css:1)가
   같은 변수를 라이트 값으로 다시 선언하면서 코드 로딩 순서상 이게 나중에
   적용돼 다크모드에서도 라이트 색이 이겨버림.
5. 🟡 **"차량 정비비" 텍스트 색** — 원본은 빨간색(`--sunday-color`), react-app은
   기본색. **원인 확정**: 원본 CSS `.summary-row.maint { color: var(--sunday-color); }`
   [style.css:1293](ubiquitous-parakeet/style.css:1293)가 정비비 행에 적용되는데
   react-app [CalendarMonthSummary.jsx](react-app/src/components/calendar/CalendarMonthSummary.jsx)의
   정비비 행엔 색상 지정이 아예 없음. **결정: 색만 빨간색으로 맞추고, 원본에
   있는 굵게(font-weight:700)는 넣지 않는다(보리 결정).**
6. 🟡 **하단 네비 "마이페이지" 글자가 "마이페이"/"지"로 줄바꿈됨** — 칸 폭은
   둘 다 60px로 동일한데, react-app의 폰트 지정(`system-ui, "Segoe UI",
   sans-serif`)이 원본(`-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto,
   "Helvetica Neue", Arial, sans-serif`)보다 짧아서, 이 환경에서 "마이페이지"를
   그리는 데 더 넓은 폭(57.6px, 원본은 52.99px)이 필요해 60px 칸에서 줄바꿈됨
   (실측 확인, 실제 사용자 기기별로 정확한 폭은 다를 수 있음).

그 외 캘린더 그리드·요일·칩·"월간 운송료 정산" 카드의 나머지 항목(거래처별
행·부가세·합계 등)은 구조·숫자 동일(③ 슬라이스에서 이미 원본과 맞춤 확인됨).

### 1-1. 메뉴(사이드메뉴) — 보리 직접 검토·확정 (2026-09-08)

> 화면 인벤토리 13개와 별개로, 모든 화면에서 공통으로 열리는 좌측 사이드메뉴.
> 보리가 직접 캡처로 짚어주고 감시관이 코드로 원인까지 확인.

1. 🟡 **"관리"↔"경영" 섹션 순서가 반대** — CSS `order` 속성으로 실제 화면
   순서가 소스 순서와 다름(처음에 DOM 순서만 보고 "같다"고 잘못 판단했었음,
   재확인함). **원본 실제 화면**: 경영→관리→서류→설정. **react-app**:
   관리→경영→서류→설정("서류"/"설정" 순서는 둘 다 동일, 이 부분만 다름) —
   [style.css](ubiquitous-parakeet/style.css) 다크/라이트 섹션에 `order` 지정,
   [SideMenu.jsx](react-app/src/components/SideMenu.jsx)엔 없음(작성 순서 그대로 노출).
2. 🔴 **"2222 일지"(소속기사 차량) 옆 톱니바퀴(빠른 설정) 버튼이 react-app에
   없음** — 원본은 `showSubCarSettings()`로 그 차량 전용 설정 화면
   (`subCarSettingsPage`, [index.html:1389](ubiquitous-parakeet/index.html:1389))
   에 바로 들어감. react-app은 이 버튼 자체가 없고, 관련 설정이 "앱 설정"
   화면([AppSettingsPage.jsx](react-app/src/components/AppSettingsPage.jsx))
   맨 아래로 옮겨가 있음. **추가로 확인된 더 큰 문제**: 원본은 메인차량과
   기사(서브)차량의 "운행 일지 세부 입력" 토글 6종(달력표시방식·결제수금·
   운행시간·플랫폼·계기판·화물톤수)을 각각 따로 가지는데, react-app은
   이 설정값이 [practiceSettings.js](react-app/src/domain/practiceSettings.js)
   기준 **메인/서브 구분 없이 딱 한 벌뿐** — 화면 위치만 옮기는 수준이 아니라
   **서브차량 전용 데이터 필드 자체를 새로 만들어야 하는 작업**. 다음 감시관이
   범위를 다시 잡아야 함.
3. 🔴 **사이드메뉴 배너 이미지 — 다크/라이트 전용 이미지 2개가 통째로 누락**
   — 원본은 `images/banner_image_Light.png`·`images/banner_image_dark.png`를
   테마별로 각각 보여주는데([index.html:108-110](ubiquitous-parakeet/index.html:108)),
   react-app `public/images/`엔 이 두 파일 자체가 없고 범용
   `banner_image.png` 하나만 씀([SideMenu.jsx](react-app/src/components/SideMenu.jsx)) —
   이미지 안에 있던 "운행 일지" 글자를 없애고 대신 `<span>` 텍스트로 옆에
   붙여서 흉내만 냄. 보리가 직접 제작한 원본 이미지 파일을 그대로 써야 함.
4. 🔴 **"{기사이름} 기사 관리" 메뉴 항목 텍스트가 "기사 기사 관리"로 중복
   표시됨** — [SideMenu.jsx:165,169](react-app/src/components/SideMenu.jsx:165)
   `${item.driverName} 기사 관리` 템플릿에서, 연동된 기사의 이름이 비어
   있으면 `driverName`이 fallback 값 `"기사"`로 채워지면서
   (`LinkedDriverManagementPage.jsx:94` `link.driverName || '기사'`) "기사
   기사 관리"라는 중복 문구가 남. 이름이 실제로 비어 있는 게 정상 데이터
   상태인지, 이름 표시 자체가 문제인지 확인 필요.

## ⚠️ 2~13번 섹션은 자동 비교 초안 — 아직 보리 검토 전

아래 2~13번(마이페이지~고객센터)은 감시관이 스크린샷·코드로 1차 조사한
초안이고, **보리가 직접 확인하기 전까지는 정확하다고 보장 못함**(홈 화면도
초안에 실제로 틀린 항목이 있었음). 홈 화면과 같은 방식으로 하나씩 다시
짚어나갈 예정.

### 2. 마이페이지
- 🟡 **개인정보 카드에 "차주" 뱃지 없음** — 원본은 "개인정보" 옆에 "차주" 색깔
  뱃지가 붙어 있는데 react-app엔 없음. (※ 참고: `STATUS.md`에 "차주/기사 뱃지
  삭제는 2026-09-05 이미 승인된 의도적 변경"이라 기록돼 있음 — 재승인 대상은
  아니고 기록 차원.)
- 🟡 **메뉴 아이콘 색상 배지 없음** — 원본은 차량관리/거래처/미수금/정비주유기타/
  운송비내역서/세금계산서 각 카드 아이콘에 파란색 원형 배경이 있어 화면이
  알록달록한데, react-app은 아이콘이 무채색(회색조)이라 화면이 밋밋해 보임.
- ⚪ **마이페이지 화면 우측 상단 햄버거 메뉴 버튼 없음** — 원본은 마이페이지에서도
  햄버거 메뉴 버튼이 보이는데, react-app은 뒤로가기만 있고 그 자리가 비어 있음
  (스크린샷으로 확인, 홈 화면엔 둘 다 있음).
- 🔴 **"초대코드로 차주 연동" 메뉴가 차주 계정에도 뜸(코드 확인 완료)** —
  `MyPage.jsx:162` 조건이 `cloud && !employed`(로그인 상태이고 "소속기사"만
  아니면 노출). "차주" 자신도 조건을 통과해버려서, 이미 차주인 계정에도
  "다른 차주 초대코드 입력" 메뉴가 보임 — 원본엔 이런 메뉴가 아예 없음(차주가
  또 다른 차주에게 연동될 이유가 없는 기능이라 원본 설계엔 없었던 것으로 보임).
  보리가 말한 "차주/기사 구분이 거의 없다"의 구체적 사례.
- (정정) "업무 바로가기" 부제 문구("자주 사용하는 관리 메뉴")는 스크린샷에서
  놓쳤을 뿐 둘 다 있음 — 차이 아님.

### 3. 매출 (전체 손익 탭)
- ⚪ "운임 수수료" 값이 0일 때 원본은 "-0원"(마이너스 부호 표시), react-app은
  "0원"(부호 없음) — 값 자체는 0이라 의미 없지만 표시 방식만 다름.
- 그 외 탭 구성(전체 손익/차주/기사)·순이익·부가세·운송 수입·운행 지출 항목·
  금액 전부 텍스트까지 완전 일치(9/8 기준 재확인 — ④에서 빼기로 한 "수치
  불일치" 건과 달리 지금은 일치, 참고 기록만).

### 4. 일일운행 (오늘 운행 일지)
- 완전 일치. "휴무/고정 노선/운행 횟수 입력/운행 일지 세부 입력/+ 운행 일지
  추가/차량 정비·주유·기타/+정비·주유·기타 추가" 문구·순서 전부 동일.

### 5. 차량 관리
- 🟡 (기존 조사 §6 재확인) 원본은 "회사 정산" 라벨이 카드에 표시되는데
  react-app은 대신 전화번호(010-1111-1111 등)를 표시 — "회사 정산" 개념
  자체가 매출제/월급제 전환으로 이제 안 쓰이는 옛 개념이라 원본 라벨 복원이
  맞는지는 보리 결정 필요(이미 백로그에 있던 항목).
- ⚪ react-app 카드엔 "수정"/"삭제" 버튼이 바로 보이는데, 원본 카드엔 그런
  버튼 문구가 안 보임(클릭하면 별도 화면이 열리는 구조로 추정, 조작 방식
  자체가 다를 수 있음 — 다음에 실제 클릭해서 확인 필요).
- 메인/기사차량 목록(11나·11가1111·22나2222), 톤수, "+ 추가" 버튼은 동일.

### 6. 거래처
- ⚪ **담당자명·"고정노선 연동" 뱃지 표시 순서가 반대** — 원본은 "홍길동 담당"이
  먼저, "고정노선 연동"이 나중. react-app은 "고정노선 연동"이 먼저, "홍길동
  담당"이 나중.
- ⚪ react-app 카드마다 "수정"/"삭제" 버튼이 바로 보이는데 원본엔 텍스트로
  안 보임(차량관리와 같은 패턴 — 원본은 클릭해서 들어가는 구조로 추정, 확인 필요).
- (참고) **"연습운수" 거래처 — 1차 조사(9/7) 땐 react-app에만 있었는데, 지금
  (9/8) 확인해보니 원본에도 똑같이 있음.** 데이터가 그 사이 바뀐 것으로 보임 —
  ④(매출 수치 조사, 이번엔 빼기로 함)와 연결된 항목이라 더 안 건드림, 참고만.
- 그 외 거래처 3곳 이름·사업자번호·연락처·결제주기 문구 전부 일치.

### 7. 미수금/정산 관리
- ⚪ react-app에만 안내 문구("운행 일지 세부 입력에서 자동으로 모읍니다. 부분
  입금은 상세에서 기록합니다.")가 있음 — 원본엔 없음.
- ⚪ 버튼 문구 — react-app "상세"/"입금 완료"(짧게), 원본 "미수금 상세"/
  "입금 완료 처리"(길게).
- ⚪ 금액 뒤 "원" 앞 띄어쓰기 — react-app "100,000 원"(띄움), 원본
  "100,000원"(붙임).
- (참고) **미수금 건수 — 지금(9/8) 확인: 원본 1건 / react-app 2건**(1차
  조사 9/7 땐 "원본 0건 vs react-app 2건"이었음, 데이터가 계속 바뀌는 중으로
  보임) — ④와 같은 종류라 안 건드림, 참고만.

### 8. 정비/주유/기타
- 🟡 **"정비/주유/기타" 탭 위치가 반대** — react-app은 탭 3개가 화면 맨
  위(타이틀 바로 아래)에 있고, 원본은 탭이 목록 맨 아래("+추가" 버튼
  다음)에 있음. 화면 구성 순서 차이.
- 🟡 **월 합계 카드 위치·문구** — react-app "이번 달 정비 / 2건 / 합계
  1,300,000원"이 목록 위(맨 위)에 먼저 나오고 항목 개수("2건")도 표시.
  원본은 "9월 정비 / 합계 1,300,000원"이 목록 아래(항목들 다음)에 나오고
  개수 표시 없음.
- ⚪ react-app 항목마다 "수정삭제" 버튼이 바로 보임(차량관리·거래처와 같은
  패턴), 원본엔 없음.
- ⚪ 금액 뒤 띄어쓰기 계속 다름(react-app "400,000 원" / 원본 "400,000원").
- 날짜별 항목 내용(엔진/카드/엔진·미션, 금액)은 동일.

### 9. 세금계산서
- 🟡 **"공급가" vs "공급가액" 레이블 다름** — react-app "공급가", 원본
  "공급가액"(더 정확한 세무 용어).
- 🟡 **합계 줄에 라벨 없음** — react-app 카드는 "공급가 350,000원 / 세액
  35,000원 / 385,000원"(세 번째 숫자에 "합계"라는 라벨이 없이 숫자만),
  원본은 "공급가액/세액/**합계** 385,000원"으로 라벨이 붙어 있음.
- 🟡 **카드별 "작성 전" 상태 뱃지 안 보임** — 원본은 개별 거래처 카드에도
  "작성 전" 상태가 표시되는데, react-app 카드엔 상단 요약("작성 전 1 / 발급
  완료 0")만 있고 카드 자체엔 상태 표시가 안 보임(재확인 필요 — 스크롤/스타일
  때문에 실제론 있는데 텍스트 추출에서 빠졌을 가능성도 있음).
- 상단 탭(매출 발행/기사 매입/수수료 발행) 개수·거래처명·건수·금액은 동일.

### 10. 운송비 내역서 (요약)
- 🔴 **차량 선택 모달이 react-app엔 없음** — 원본은 진입 시 "출력할 차량을
  선택해 주세요"(메인차량 11나 / 기사차량 22나2222) 모달이 뜨는데, react-app은
  이 모달 자체가 없이 항상 메인 차량 리포트로 바로 감 — 서브차량(기사) 리포트를
  볼 방법이 없음. (이미 `STATUS.md` "후속 nit"에 기록된 문제, 이번에 원본
  화면·문구까지 실측 확인.)
- ✅ **메인 차량 리포트 본문은 완전 일치**(이번 세션 ③-3/③-4로 손본 부분) —
  날짜별 표(9월1일 7회 350,000원 등)·월간 총 운행거리·기본 운송료·거래처별
  운송료·부가세·계 전부 텍스트까지 정확히 동일. 이관 작업이 제대로 됐음을
  이 비교로도 재확인.

### 11. 개인정보
- 🟡 **상단 안내 문구 내용이 다름** — react-app "입력하면 바로 저장됩니다.
  소속 연결은 나중에 붙입니다.", 원본 "입력한 내용은 운송비 내역서와 정산
  정보에 사용됩니다." — 같은 자리인데 서로 다른 내용을 안내함.
- ⚪ "04 계정" 섹션 부제 — react-app "로그인 상태", 원본 "로그인 상태와 계정
  연결 관리"(더 김). react-app엔 "테스트 계정으로 사용 중입니다." 문구가
  있고 원본엔 없어 보임(재확인 필요).
- 01~03 섹션(사업자 정보/대표자·연락처/정산 계좌) 항목 구성은 동일.

### 12. 기사연동관리 (기존 §11 실측 상세)
- 🔴 **안내 문구 완전히 다름** — react-app "한 차량은 한 기사에게만 할당할
  수 있습니다. 로그인한 계정은 클라우드에도 저장됩니다.", 원본 "기사님이
  초대 코드를 입력하면 실시간으로 연결됩니다." — 내용 자체가 다름.
- 🔴 **입력 폼 노출 방식이 다름** — 원본은 "기사 초대·할당" 섹션(이름·전화번호·
  초대코드·코드생성·문자발송·할당차량·할당 시작일/종료일·초대 저장)이 화면에
  항상 펼쳐져 있음. react-app은 폼이 안 보이고 "+ 초대" 버튼만 있어 클릭해야
  열리는 구조로 추정(다음에 실제 클릭해서 재확인 필요).
- 🟡 **목록 섹션 타이틀 없음** — 원본은 "기사 연결 현황 / 연동 상태와 차량
  할당 기간을 관리합니다."라는 섹션 설명이 있는데 react-app엔 없음.
- 🟡 **버튼 문구 전부 다름** — react-app "수정/연동 완료/삭제", 원본 "연결
  상태 확인/초대 수정/초대 취소". 이름만 다른 건지 실제 동작도 다른지 확인 필요.
- 🟡 **항목 표시 형식 차이** — react-app은 "11가1111 · 2026-09-07 · 종료일
  없음"처럼 한 줄로 압축. 원본은 "할당 차량/할당 기간/할당 중" 상태뱃지까지
  라벨을 붙여 여러 줄로 표시.
- ✅ 통계(연동 중 0 / 초대 대기 1)·초대코드 값(323630)은 지금은 일치.

### 13. 고객센터
- ✅ 완전 일치(탭 3개, FAQ 4개 질문 문구까지 전부 동일).

## 진행 상태

**차주 + 라이트 + 모바일 = 13개 화면 중 12개 완료(로그인 화면 제외, 게스트
패스에서 자연스럽게 확인 예정).** 아직 안 한 것: 다크 테마 재확인, 게스트
계정 재확인, 로그인 화면.
