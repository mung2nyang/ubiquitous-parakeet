# UI 이관 계획 — 원본 vs react-app 

> 보리가 직접 찾은 내용을 그대로 기록한 원본 vs react-app UI 이관 계획이다.
> 착수지시서는 각섹션 진행할때 작성한다.
> UI 이관 계획은 번호 순대로 진행한다.
> 완료분은 `[x]` 표시로 보존, 진행 순서를 허가없이 변경 하지말것.
> 각 섹션 진행 중 UI 버그 및 추가 내용 발견시 기재한다.
>
> **필수 원칙(보리 지시, 2026-09-10): 이 목록을 착수할 때, 화면을
> 방문해 UI 대조·수정하는 김에 그 화면 전용 CSS를 `side-menu.css`
> (2,089줄, 10개+ 화면이 뒤섞인 거대 파일)에서 함께 떼어낸다.** CSS
> 분리를 UI 이관과 별개 세션으로 나중에 따로 하면 같은 화면을 두세 번
> 열어봐야 해서 시간이 너무 오래 걸림 — **반드시 "이 화면 UI 대조 +
> 이 화면 CSS 분리"를 한 슬라이스로 묶어 진행한다.** (참고: 화면
> 공통으로 쓰이는 진짜 중복 CSS(form-group/input-box·모달 shell)는
> 2026-09-10에 이미 정리 완료(git 이력). 이제
> `side-menu.css`에 남은 건 대부분 화면별 전용 스타일.)
> **CSS 분리 중 죽은 CSS를 발견하면**(어느 JSX에서도 안 쓰는 규칙),
> 그 자리에서 지우지 말고 **그 화면 섹션 항목 바로 아래에 위치·내용을
> 적어둔다**(이관 완료 후 한꺼번에 처리 — `STATUS.md` "이관 완료 시
> 처리할 숙제"에도 동기화).
>
> **완료 화면 + CSS 분리 현황**(각 화면 CSS를 `side-menu.css`에서
> 전용 파일로 뽑아낸 결과, 정확한 줄수는 보리 개인 메모 참고):
> - 마이페이지(구 §2, `docs/report.md` §1, 2026-09-10) — `mypage.css`로
>   분리 완료. **죽은 CSS 2개 남아있음**(`.mypage-header-spacer`·
>   `.mypage-role-pill`, 이관 완료 후 정리 예정 — `STATUS.md` 참고).
> - 매출(구 §3, `docs/report.md` §2, 2026-09-10) — `revenue.css`로
>   분리 완료.
> - 일일운행(§4, 아래 참고) — `day-log-shell.css`·`day-log-expenses.css`
>   등 `components/day-log/` 여러 파일로 분리 완료.
> - 차량 관리(§5, 아래 참고, 2026-09-14) — `car-management.css`로
>   분리 완료(§2-1-C `[x]`, 슬라이스 ID는 당시 옛 번호 그대로 보존).
>   죽은 CSS 2개 남아있음(아래 §5 항목 참고).
> - 정비/주유/기타(§8, 아래 참고, 2026-09-15) — `maint-fuel.css`로
>   분리 완료(하단 dock·아이콘·칩 스타일 전부 포함). 죽은 CSS 없음.
> - 리포트/운송비 내역서(§10, `docs/report.md` 슬라이스 G, 2026-09-15) —
>   `components/report/report.css`로 **CSS만** 분리 완료(`cf54899`).
>   **§10 UI 내용 대조는 아직 `[ ]` 미착수**(아래 §10 참고) — 원래 원칙인
>   "UI 대조 + CSS 분리를 한 슬라이스로" 예외 케이스, 상세는 아래
>   "`side-menu.css` 잔여 경계 지도" 참고.
> - 세금계산서(§9, 2026-09-16) — `tax-invoice.css`로 UI 대조 + CSS
>   분리 전부 완료(H 블록 해소, J 블록 중 §9 몫인 `tax-invoice-guide`도
>   이동, `driver-code-row`는 §12 몫이라 의도적으로 안 옮김).
>   **다음은 §10(운송비 내역서) UI 대조**(CSS는 이미 분리됨, 위 참고).
>
> **`side-menu.css` 잔여 경계 지도**(2026-09-15 grep 전수조사, `docs/report.md`
> 커밋 `2528233`/`7854094` — 이 표가 정본, 아래는 그 요약): `side-menu.css`에
> 아직 안 나뉜 코드를 13개 블록(A~M)으로 나눔. **이 정리는 위 §1~§14 순서와
> 별개 트랙이 아니다** — 특정 화면 하나에만 속한 블록은 그 화면의 §차례가
> 왔을 때 "UI 대조 + CSS 분리"로 묶어 처리하고(원칙 그대로), 여러 화면이
> 진짜로 공유하는 블록만 §순서와 무관하게 별도 "공용 CSS" 트랙으로 뺀다.
>
> | 블록 | 내용 | 해당 §/성격 | 상태 |
> |---|---|---|---|
> | A | 사이드메뉴 자체 | 사이드메뉴(공용, 1-1 완료분과 별개 CSS) | 보류 |
> | B | 관리화면 공용 배지/카드 | 여러 화면 공유 — 진짜 공용 | 보류(공용 파일 필요) |
> | C | 차량 커미션 잔재 | §5 차량관리 | §5는 완료됐으나 이 CSS는 아직(죽은 CSS 2개 포함, 이관 후 처리) |
> | D | 개인정보 일부 | §11 개인정보 | §11 차례에 처리 |
> | E | 설정 공용 | 여러 화면 공유 — 진짜 공용 | 보류 |
> | F | 정비/주유/기타 위젯 | 12개 파일 공유 확인 → 진짜 공용 | **제외 확정**(§8 전용 아님, 2026-09-15) |
> | G | 리포트/운송비내역서 | §10 | **CSS만 완료**(`cf54899`), UI 대조는 미착수 |
> | H | 세금계산서 금액그리드 | §9 | **완료**(`tax-invoice.css`로 이동, 2026-09-16) |
> | I | 알림 | 전 화면 공통 헤더(1-2 항목과 연관) | 보류 |
> | J | 기사연동관리+세금계산서 가이드 | §9·§12 걸침 | §9 몫(`tax-invoice-guide`) **완료**, `driver-code-row`(§12 몫)만 남음 — §12 차례에 처리 |
> | K | 앱설정 프리셋 | §13 앱 설정 | §13 차례에 처리 |
> | L | 고객센터 | §14 고객센터 | §14 차례에 처리 |
> | M | 메시지설정 | §13 하위(너무 작음) | 보류 |
>
> **앞으로 원칙**: 화면별 블록(C/D/G/H/J/K/L)은 §순서를 어기지 않는다 —
> G가 §10보다 먼저 CSS만 떨어져 나간 건 이미 벌어진 예외로 기록만 남기고,
> H·J(§9 몫)는 2026-09-16 완료. 다음 `side-menu.css` 작업은 D(§11)·
> K(§13)·L(§14) 각 화면 차례가 왔을 때 진행한다. B·E·F 같은 진짜 공용
> 블록만 §순서와 무관하게 별도 시점에 정리(보리 결정 2026-09-16).


## 방법

- 두 앱을 같은 크기(모바일 375×812)로 띄워 화면별 스크린샷 대조.
- 라이트/다크 테마 각각.
- 계정: 차주(현재 로그인된 테스트 계정, "테스트님") → 게스트("비회원으로 시작").
- "잘림/겹침처럼 보이는 것"은 스크린샷만으로 단정하지 않고 `document.body.scrollWidth`
  등 코드로 교차검증 후 기록(브라우저 도구 자체의 캡처 확대 때문에 실제로는
  문제없는 경우가 있었음 — 방법 검증 과정에서 확인).

## 공용 버그 — `.toggle-btn` 라운드 처리 불일치 `[x]` 완료(2026-09-16, `eba8eb1`)

**보리 지시**: 알약 모양(24px, 완전히 둥긂)으로 나오면 안 되는 자리인데
나오고 있음 — 앱 안에서 "어떤 칩은 알약, 어떤 칩은 살짝 둥근 사각형"으로
섞여 보이는 게 문제. 알약 모양이 맞는 건 `<년 월>` 날짜 선택 칩류뿐이고,
아래 탭/토글류는 전부 살짝 둥근 사각형(§8 정비/주유/기타처럼)이어야 함.

**원인**: 공용 기본값 `shared-controls.css:159` `.toggle-btn { border-radius: 24px; }`.
§8(정비/주유/기타)만 `MaintFuelPage.jsx:214`에서 `maint-fuel-tabs`와
`maint-management-tabs` 클래스를 **같이** 붙여서
`maint-fuel.css:76-80` `.maint-management-tabs .toggle-btn { border-radius: 12px; }`
오버라이드를 적용받는데, 아래 화면들은 `maint-fuel-tabs`만(또는
`settings-segmented-control`만) 붙어 있어서 오버라이드가 안 걸리고 기본
24px(알약)로 남음 — §8 마크업을 복사하면서 클래스 하나씩 빠뜨린 것으로
추정.

**동일 원인으로 확인된 위치(2026-09-16 `grep` 전수조사)**:
- `TaxInvoiceToolbar.jsx:48`(§9, 매출발행/기사매입/수수료발행 탭)
- `TaxInvoicePage.jsx:163`(§9, 작성 전/발급 완료 토글)
- `OwnerRevenueView.jsx:114`(§3, 년 매출/월 매출 — **§3은 이미 `[x]`
  완료 처리된 화면인데 이 버그가 남아있었음**)
- `DriverRevenueView.jsx:62`(§3, 년 매출/월 매출, 기사용 뷰)
- `CarDriverConnectPanel.jsx:19`(§5, 기사 연동 탭 — **§5도 이미 `[x]`
  완료 처리된 화면**, 자체 클래스 `car-driver-connect-tabs`엔
  radius 오버라이드 없음)
- `AppSettingsPage.jsx:133`(§13, 달력 일일 표시 방식 횟수/금액 —
  `settings-segmented-control`만 있고 `maint-fuel-tabs`/
  `maint-management-tabs` 둘 다 없어 역시 기본 24px)

**처리 방침(2026-09-16 재확인, 착수 승인 완료 — `docs/report.md` 착수지시서
참고)**: 처음엔 6곳 전부에 `maint-management-tabs`를 붙이는 안을
생각했으나, 그 클래스 자체가 `display:grid; grid-template-columns: 1fr 1fr 1fr;`
(`maint-fuel.css:69-74`)라서 버튼이 3개인 §9 매출발행 탭에만 맞고,
나머지 5곳(작성전/발급완료·년매출/월매출×2·기사연동 탭·횟수/금액)은
전부 **버튼 2개**라 그대로 붙이면 그리드 3칸 중 1칸이 비어 레이아웃이
깨짐 — 폐기.

대신 6곳 전부가 공통으로 걸고 있는 `settings-segmented-control` 클래스가
정확히 이 6곳에서만 쓰인다는 걸 확인(`grep -rn "settings-segmented-control" src`
결과, 다른 소비처 없음) → **`side-menu.css:603-606`
`.settings-segmented-control .toggle-btn` 규칙에 `border-radius: 12px`
한 줄만 추가**하면 JSX 변경 없이 6곳 전부 안전하게 고쳐짐(§8은
`settings-segmented-control` 클래스가 없어 무영향).

## 발견 사항 (차주, 라이트, 모바일)

### 1. 홈(캘린더) — 보리 직접 검토·확정·완료`[x]` (2026-09-08)

> 아래 6개는 보리가 직접 화면 캡처로 짚어주고, AI가 코드로 원인까지
> 확인한 뒤 확정한 것. 최초 자동 비교로 나왔던 "햄버거 메뉴 버튼 배경 차이"
> 항목은 실제로 동일해서(원본도 투명 배경) 삭제.

1. **"년"/"월" 옆 아래 화살표(▼)** — react-app에만 있음("2026년⌄ 9월⌄"),
   원본은 순수 텍스트만("2026년 9월").
2. **상단 헤더가 원본보다 위로 붙어있음** — 벨·로고·햄버거 메뉴, 날짜
   선택 알약까지 포함한 헤더 블록 전체가 react-app이 원본보다 위쪽에 바짝
   붙어있음(원본 대비 날짜 알약 시작 위치가 약 50px 정도 차이). 전체적으로
   아래로 내려야 함.
3. **벨/햄버거 아이콘 원형 배경 — 확인 결과 문제 아님.** 원본도 투명
   배경이라 육안으로는 안 보임, react-app도 마찬가지 — 실질적으로 동일.
   손대지 않음(보리 확인).
4. **날짜 칸 클릭 시 하이라이트가 다크모드에서 흰색으로 뜸** — 원본은
   눌렀을 때 보기 편한 회색인데 react-app은 흰색이 튐. **원인 확정**:
   다크모드 배경색 변수(`--hover-bg`/`--today-bg`)가 [account-flow.css:25-36](react-app/src/account-flow.css:25)
   에서만 어두운 값으로 재정의되는데, [main-calendar.css:1-14](react-app/src/main-calendar.css:1)가
   같은 변수를 라이트 값으로 다시 선언하면서 코드 로딩 순서상 이게 나중에
   적용돼 다크모드에서도 라이트 색이 이겨버림.
5. **"차량 정비비" 텍스트 색** — 원본은 빨간색(`--sunday-color`), react-app은
   기본색. **원인 확정**: 원본 CSS `.summary-row.maint { color: var(--sunday-color); }`
   [style.css:1293](ubiquitous-parakeet/style.css:1293)가 정비비 행에 적용되는데
   react-app [CalendarMonthSummary.jsx](react-app/src/components/calendar/CalendarMonthSummary.jsx)의
   정비비 행엔 색상 지정이 아예 없음. **결정: 색만 빨간색으로 맞추고, 원본에
   있는 굵게(font-weight:700)는 넣지 않는다(보리 결정).**
6. **하단 네비 "마이페이지" 글자가 "마이페이"/"지"로 줄바꿈됨** — 칸 폭은
   둘 다 60px로 동일한데, react-app의 폰트 지정(`system-ui, "Segoe UI",
   sans-serif`)이 원본(`-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto,
   "Helvetica Neue", Arial, sans-serif`)보다 짧아서, 이 환경에서 "마이페이지"를
   그리는 데 더 넓은 폭(57.6px, 원본은 52.99px)이 필요해 60px 칸에서 줄바꿈됨
   (실측 확인, 실제 사용자 기기별로 정확한 폭은 다를 수 있음).

그 외 캘린더 그리드·요일·칩·"월간 운송료 정산" 카드의 나머지 항목(거래처별
행·부가세·합계 등)은 구조·숫자 동일(③ 슬라이스에서 이미 원본과 맞춤 확인됨).

### 1-1. 메뉴(사이드메뉴) — 보리 직접 검토·확정·완료`[x]` (2026-09-08)

> 화면 인벤토리 13개와 별개로, 모든 화면에서 공통으로 열리는 좌측 사이드메뉴.
> 보리가 직접 캡처로 짚어주고 AI가 코드로 원인까지 확인.

1. **"관리"↔"경영" 섹션 순서가 반대** — CSS `order` 속성으로 실제 화면
   순서가 소스 순서와 다름(처음에 DOM 순서만 보고 "같다"고 잘못 판단했었음,
   재확인함). **원본 실제 화면**: 경영→관리→서류→설정. **react-app**:
   관리→경영→서류→설정("서류"/"설정" 순서는 둘 다 동일, 이 부분만 다름) —
   [style.css](ubiquitous-parakeet/style.css) 다크/라이트 섹션에 `order` 지정,
   [SideMenu.jsx](react-app/src/components/SideMenu.jsx)엔 없음(작성 순서 그대로 노출).
2. **"2222 일지"(소속기사 차량) 옆 톱니바퀴(빠른 설정) 버튼이 react-app에
   없음** — 원본은 `showSubCarSettings()`로 그 차량 전용 설정 화면
   (`subCarSettingsPage`, [index.html:1389](ubiquitous-parakeet/index.html:1389))
   에 바로 들어감. react-app은 이 버튼 자체가 없고, 관련 설정이 "앱 설정"
   화면([AppSettingsPage.jsx](react-app/src/components/AppSettingsPage.jsx))
   맨 아래로 옮겨가 있음. **추가로 확인된 더 큰 문제**: 원본은 메인차량과
   기사(서브)차량의 "운행 일지 세부 입력" 토글 6종(달력표시방식·결제수금·
   운행시간·플랫폼·계기판·화물톤수)을 각각 따로 가지는데, react-app은
   이 설정값이 [practiceSettings.js](react-app/src/domain/practiceSettings.js)
   기준 **메인/서브 구분 없이 딱 한 벌뿐** — 화면 위치만 옮기는 수준이 아니라
   **서브차량 전용 데이터 필드 자체를 새로 만들어야 하는 작업**. 다음 세션이
   범위를 다시 잡아야 함.
3. **사이드메뉴 배너 이미지 — 다크/라이트 전용 이미지 2개가 통째로 누락**
   — 원본은 `images/banner_image_Light.png`·`images/banner_image_dark.png`를
   테마별로 각각 보여주는데([index.html:108-110](ubiquitous-parakeet/index.html:108)),
   react-app `public/images/`엔 이 두 파일 자체가 없고 범용
   `banner_image.png` 하나만 씀([SideMenu.jsx](react-app/src/components/SideMenu.jsx)) —
   이미지 안에 있던 "운행 일지" 글자를 없애고 대신 `<span>` 텍스트로 옆에
   붙여서 흉내만 냄. 보리가 직접 제작한 원본 이미지 파일을 그대로 써야 함.
4. **"{기사이름} 기사 관리" 메뉴 항목 텍스트가 "기사 기사 관리"로 중복
   표시됨** — [SideMenu.jsx:165,169](react-app/src/components/SideMenu.jsx:165)
   `${item.driverName} 기사 관리` 템플릿에서, 연동된 기사의 이름이 비어
   있으면 `driverName`이 fallback 값 `"기사"`로 채워지면서
   (`LinkedDriverManagementPage.jsx:94` `link.driverName || '기사'`) "기사
   기사 관리"라는 중복 문구가 남. 이름이 실제로 비어 있는 게 정상 데이터
   상태인지, 이름 표시 자체가 문제인지 확인 필요.

### 1-2. 공용 헤더 스타일 — 보리 직접 검토·확정·완료`[x]` (2026-09-09, 2차)

- **화면 우측 상단 햄버거 메뉴 버튼이 없거나 스타일이 안 맞음** — 지금
  뒤로가기 버튼(투명 배경 아님, 아이콘 안에 들어간 스타일)에 맞춰서
  **홈 화면 포함 전체**를 통일해야 함. 알람벨 아이콘도 뒤로가기 버튼과
  같은 스타일로 맞추기.
- **적용 대상(햄버거 버튼이 있어야 하는 모든 화면)**: 마이페이지, 차량
  관리, 거래처, 미수금/정산, 정비/주유/기타, 세금계산서, 운송비 내역서,
  개인정보, 기사 연동 관리, 앱 설정, 공지 등 **전 화면 공통**.

### 2. 마이페이지 — 보리 직접 검토·확정·완료`[x]` (2026-09-10)

1. **"앱 설정" 좌측 톱니바퀴 아이콘 아래쪽이 찌그러져 보임** — 보리가
   스크린샷으로 짚어줘서 재조사한 결과 CSS 문제가 아니라 아이콘
   SVG `d`(도형 경로) 값 자체의 오타(마이너스 부호 1개 누락, 곡선
   제어점 좌표가 반대 방향으로 틀어짐)였음. 원본
   [index.html:156](ubiquitous-parakeet/index.html:156)의 경로 문자열과
   문자 단위로 대조해 정정(react-app `8c1ccc7`).
2. **마이페이지 전용 CSS 219줄을 `side-menu.css`에서
   [mypage.css](react-app/src/components/mypage.css)로 분리** —
   `MyPage.jsx` 외 소비처 0개 확인 후 이동. 죽은 CSS 2개
   (`.mypage-header-spacer`·`.mypage-role-pill`, JSX 소비처 0)는 기존
   관례대로 삭제 안 하고 그대로 옮김(정리는 이관 완료 후,
   `STATUS.md` "이관 완료 시 처리할 숙제" 참고).

### 3. 매출 — 보리 직접 검토·확정·완료`[x]` (2026-09-10)

1. **"운임 수수료" 값이 0일 때 원본은 "-0원"(마이너스 부호), react-app은
   "0원"** — 원본 [finance.js:1104](ubiquitous-parakeet/finance.js:1104)는
   음수 0(`-0`)을 그대로 `toLocaleString()`해 "-0원"으로 표시하는데,
   react-app [revenueFormat.js](react-app/src/components/revenue/revenueFormat.js)의
   `won()` 함수는 `|| 0` 방어 때문에 `-0`이 양수 0으로 바뀌어 부호가
   사라졌음. `NaN`일 때만 0으로 치환하고 `-0`은 보존하도록 수정
   (react-app `1a5d702`).
2. **매출 전용 CSS 265줄을 `side-menu.css`에서
   [revenue.css](react-app/src/components/revenue/revenue.css)로 분리**
   — 265줄로 기존 관례 상한(~250줄)을 15줄 넘지만, 화면 전용 스타일
   응집도 우선 원칙(§6 예외)으로 분리설계 없이 그대로 진행(보리 승인).

### 4. 일일운행 (오늘 운행 일지) — 보리 직접 검토·확정·완료`[x]`

> 2026-09-10 AI가 원본(localhost:8791)·react-app(localhost:5174) 두 화면을
> 실제로 띄워 코드+실측 대조 완료(보리가 직접 훨씬 상세하게 기록해준 목록
> 기준). 아래 A~E로 재정리.
>
> **§4 사실상 마무리(2026-09-14 기준) — A~D·E-1·F-1~F-3·디자인 토큰
> 전부 `[x]`. 남은 토글 1건은 이관 완료 후로 의도적 이월(아래 E 참고),
> §4 안에서 더 막힌 건 없음 — §5(차량 관리)로 넘어갈 수 있음.**
>
> - A·B·C·D(시간입력 위젯 포함 전체)·F-1(부가세 해제 굵기)·F-2(다크모드
>   자동완성)·F-3(콜상세 아이콘/스핀버튼/별표 SVG) 전부 확정(react-app
>   `e65b2c0`/`b14fcd8`/`c395ab3`/`93a4f2c`/`8ced672`/`ed83703`/`83de8e9`,
>   CI 초록·보리 확인 완료).
> - **E. 정비/주유/기타 패널** — 스타일 4건은 E-1(`9e6f93d`)로 `[x]`.
>   **남은 토글 닫기 기능 1건은 `InlineSheet` 리팩토링과 묶여 이관
>   완료 후로 이월**(원본에도 있는 기능이지만 UI 대조 관점에선 작은
>   차이라 급하지 않음, 아래 E 항목 참고).
>
> **디자인 토큰(CSS 변수) 1곳 통합 `[x]` 완료(§4와 별개 작업, 보리
> 지시 2026-09-14).** `react-app/src/`를 `styles/`·`components/ui/`·
> `features/` 3계층으로 재개편하는 안을 검토했으나 기존 도메인별
> `components/` 경계를 흔드는 전면 개편이라 위험 판단, 보류. 대신
> `:root` 변수가 흩어진 3개 파일(`account-flow.css`·`app-shell-base.css`·
> `side-menu.css`, 총 28개)을 `variables.css` 1개로 합치는 저위험
> 슬라이스만 진행 — `--shadow-sm`은 승자 값만 채택. react-app
> `ef82306`, CI 초록·AI 재검증·보리 브라우저 실검증 완료. **원칙 확정
> (보리): "라이트 전용만 있는 건 다크모드도 적용해야 한다, 앱
> 통일을 위해서"** — 앞으로 비슷한 비대칭 발견 시 이 원칙대로.

**A. `[x]` 완료 — 콜상세 폼 CSS 4건** (react-app `c6c831b`, 보리 확인 2026-09-10.
2026-09-10 재조사로 1차 원인 2곳 정정됨: 1차엔 원본 모달 버전만 보고 판단했는데
실제 화면은 원본 인라인 아코디언 버전 CSS를 써서 값이 다름)
- 헤더 "**N일** 일지 세부 입력" 날짜 숫자 누락(react "운행 일지 세부 입력").
- 입력창 포커스 시 파란 라운드 테두리 전체 누락(앱 전역 버그, `.form-group label`
  전역 글자크기 규칙도 같이 누락 — 둘 다 콜상세 폼 한정 아니라 앱 전체 영향).
- 계기판 "km" 단위가 입력창 **안쪽**(원본 `position:absolute`)에 있어야 하는데
  react는 입력창 **바깥**(flex)에 있음.
- 레이블 글자 크기 — 원본은 "전부 통일"이 아니라 두 단계 설계(`.form-group` 레이블=
  fs-3, `.call-detail-panel` 직계 레이블=fs-2)인데 react는 전역 fs-3 규칙 자체가
  없고 "계산서" 레이블 스코프도 빠져 있음. `.load-label`/`.unload-label`
  `font-weight:800`은 원본에도 있어 **문제 아님**(1차 오판 정정).

**B. `[x]` 완료 — 콜상세 폼 누락된 기능 3건**
- 상차지/하차지 즐겨찾기 칩(`#callLocShortcuts`) — react-app `6b4a9b7`, 보리 `[x]` 2026-09-11.
- 거래처 입력창 옆 "+ 추가" 버튼 — react-app `0db5bde`, 보리 `[x]` 2026-09-11.
- "산재보험료" 입력 필드 — react-app `1de2537`, 보리 `[x]` 2026-09-11.

**C. `[x]` 완료 — 레이아웃 위치 전반적으로 틀어짐** (2중 스크롤·카드 이탈
근본 수정 `796bea6`/`387e3c3`, 보리 확인 2026-09-10. 아래는 3차 재조사로
정체를 밝힌 기록)
- AI가 처음엔 개별 필드 2열 그리드만 보고 "재현 안 됨"으로 잘못 결론냈던
  항목. 보리가 스크린샷으로 다시 짚어줘서 재조사한 결과, **이 항목은 별개
  버그가 아니라 아래 두 구조적 버그 + B그룹(누락 기능)을 뭉뚱그려 가리킨
  것**이었음 — 상세는 `docs/report.md` §2-1·§2-2:
  1. **일일운행 화면 2중 스크롤** — `main-app-container`의 고정 패딩(128px)과
     `work-log-page`의 `height:100dvh`가 안 맞아 body에도 스크롤 발생.
  2. **"N일 일지 세부 입력" 카드 이탈** — 원본은 제목·목록·펼치는 폼이 한 카드
     (`modal-section`) 안인데, react는 `CallDetailList`가 카드를 닫아버리고
     `InlineSheet`(콜상세 폼)가 형제로 분리 렌더링돼 카드 밖으로 나감.
  3. B그룹(즐겨찾기 칩·거래처 +추가 버튼·산재보험료 필드 등 누락 기능)도
     "레이아웃이 다르다"는 인상에 포함된 것으로 보임.

**D. 항목별 결과 — 전부 `[x]` 완료(아코디언·토글·자동채움·시간입력 전체)**
- `[x]` 완료 — ~~아코디언 슬라이드 애니메이션 — 이미 구현됨~~ **정정(2026-09-11): 실제로는
  안 보이던 진짜 버그였음.** 아래 항목들이 순서대로 원인이었다 — ①
  `grid-template-rows: 0fr↔min-content`은 트랙 타입이 달라 애초에 보간
  불가(discrete 점프) ② 닫기 시 자식을 동기 언마운트해 콘텐츠 기준
  높이가 즉시 0이 됨 ③ 닫히는 동안 overflow가 안 바뀌어 내용이 잘려
  "말리는" 것처럼 보임 ④ 열기는 클립 자체가 없어 내용이 즉시 다 그려짐
  ⑤ "종류 선택→폼" 전환은 `open` 값이 안 바뀌어 전환 자체가 안 걸림
  ⑥ 부모가 내용물을 이중 조건부 렌더링해 닫기 시 wrapper는 살아있어도
  내용이 먼저 사라짐. 6차에 걸쳐 전부 수정, 보리 브라우저 확인 완료
  (2026-09-11, react-app `67762ff`~`6c5ba8d`/`3ae7db9`).
  구조적 한계 기록은 `docs/sot.md` §4-11.
- `[x]` 조치 불필요 — "+추가"/분류칩(플랫폼·계산서·거래처) 재클릭 시 닫기(토글) —
  콜상세 폼 쪽은 이미 구현됨(코드 확인).
- `[x]` 조치 불필요 — "직전 항목과 동일하게 채우기" 버튼 — 이미 있음(신규
  항목엔 원래 안 보이는 게 정상, 이전 항목 있을 때만 나타남).
- `[x]` 완료 — 시간입력: 원본 `ui-widgets.js`의 `app-temporal` 커스텀
  날짜/시간 위젯 시스템을 공용 `TemporalInput` 컴포넌트로 신규 구현
  (D-0~D-3, react-app `e65b2c0`/`b14fcd8`/`c395ab3`/`93a4f2c`, CI
  초록·보리 확인 완료).

**E. 정비/주유/기타(일일운행 안 인라인 패널) — 스타일 4건 `[x]`(E-1).
남은 토글 1건은 이관 완료 후로 이월(2026-09-14, 보리 결정).**
- `[x]` 완료 — 날짜·누적거리 2단 그리드(50:50)·분류 칩 사각 라운드·
  결제방식 세그먼트 컨트롤·날짜 트리거 아이콘 삭제 (E-1, react-app
  `9e6f93d`, CI 초록·보리 확인 완료).
- `[ ]` **이관 완료 후로 이월** — "+추가"/분류칩 재클릭 시 닫기(토글)
  기능 없음(정비/주유/기타 패널 쪽, D의 콜상세 폼과 별개). **원본에도
  있는 기능이지만**, 구현하려면 `useExpenseForm.js`의 `setTimeout(420)`
  (`InlineSheet` 리팩토링 대상, `docs/sot.md` §4-11과 같은 구조적 지점)을
  건드리게 돼 그 리팩토링과 묶어서 이관 완료 후 같이 처리하기로 함
  (STATUS.md "이관 완료 후 진행사항" 참고). **UI 대조 관점에서는 작은
  차이라 급하지 않음** — 토글 안 되는 것뿐이고 기능 자체(추가/수정)는
  정상 동작.

**F. 새로 발견된 항목 — F-1·F-2·F-3 전부 `[x]` 완료**
- `[x]` **F-1. "부가세 해제" 레이블 글자 굵기 누락** — 원본
  `style.css:3283-3284`의 `.call-vat-row > label:first-child { font-weight: 750; }`
  (일반 규칙)를 react-app 이식(`3ae7db9`) 때 빠뜨림. `call-detail-form.css`의
  `.work-log-page .call-vat-row > label:first-child`에 `font-weight: 750;`
  1줄 추가(react-app `8ced672`, CI 초록·보리 확인 완료).
- `[x]` **F-2. 다크모드에서 드롭다운으로 값 고르면 입력창이 라이트모드처럼
  밝아짐** — 원인: 크롬이 `<datalist>` 선택값을 자동완성으로 취급해
  `:-webkit-autofill` 강제 스타일(밝은 배경+검은 글자)을 입힘. 앱 전체
  `list=` 입력창 2곳(`#callClient` 거래처, `#drvCar` 차량번호) 수정
  (`shared-controls.css`, react-app `ed83703`, CI 초록·보리 확인 완료).
  (참고: 앱
  전체에 `color-scheme` CSS가 한 번도 선언된 적 없다는 것도 확인됨 —
  네이티브 달력·시간 팝업이 다크 테마와 무관하게 브라우저 기본값(라이트)로
  뜨는 별개 이슈, 필요하면 나중에 추가.)
- `[x]` **F-3. 콜상세 폼 아이콘/스핀버튼/즐겨찾기 SVG 통일**[2026-09-14
  신규 지시] — 화물 톤수 스핀 버튼 삭제, 시간 선택 시계 아이콘·입금예정일
  달력 아이콘 삭제(E-1의 `.app-temporal-icon { display:none }` 패턴
  재사용), 상/하차지 즐겨찾기 별표(★/☆ 유니코드)를 앱 SVG 아이콘 스타일로
  통일(`StarIcon` 신규, `.is-filled` 토글). react-app `83de8e9`, CI
  초록·보리 확인 완료.

### 5. 차량 관리 — 전부 `[x]` 완료 (2026-09-14~15)
- `[x]` 완료 — 카드에 표시되는 전화번호 제거, 원본처럼 "기사 연동"인지
  "운행 일지"인지 라벨칩 표시(§2-1-A, react-app
  `8641be3`/`032e807`/`15bfc43`).
- `[x]` 완료 — 원본 카드는 수정/삭제가 아이콘인데 react-app 카드는
  "수정"/"삭제" 글자로 되어 있던 것을 아이콘으로 복원(적용 대상:
  차량관리·거래처·정비/주유/기타 공통, §2-1-B, react-app
  `489e9fd`/`a0f6d6a`).
- **CSS 분리(§2-1-C, `car-management.css`) 조사 중 죽은 CSS 2개
  발견**(`side-menu.css`): `car-commission-heading`+`strong`+`span`
  (336~353행, 18줄)·`car-daylog-preview`+`li`(461~479행, 19줄) — 둘 다
  어느 JSX에서도 안 씀. 지금 안 지우고 이관 완료 후 처리(`STATUS.md`
  동기화됨).

### 6. 거래처 `[x]` 완료 (2026-09-15)
- 고정노선 연동 칩이 원본은 파란색인데 react-app은 회색임[확인
  2026-09-15, 보리: 문제없음, 안 건드림].
- **수수료·파렛트 배지 자체가 react-app에 없던 이관 누락**[발견·수정
  2026-09-15] — 원본은 즐겨찾기→수수료→고정노선연동→파렛트 4종 배지가
  있는데 react-app엔 즐겨찾기·고정노선연동 2개만 있어 수수료/파렛트
  값을 입력해도 화면에 아무 표시가 없었음. `ClientListItem.jsx`에 두
  배지 추가(react-app `1a3d6aa`). 파렛트는 원본처럼 값 없이 "파렛트"만
  표시하면 의미 없다는 지적(보리)에 따라 원본과 다르게 "파렛트
  {금액}원"으로 값을 붙임(원본과 다른 부분, 명시).
- **수수료 칩만 색이 달라 일관성 없었던 것 수정**[2026-09-15] —
  `.management-badge.commission` 전용 색상 규칙 삭제, 나머지 두 칩과
  같은 기본 회색으로 통일(react-app `d3585db`).
- **배지 위치 고정(진행 중, 미커밋)**[2026-09-15] — 배지 4종이 한 줄에
  조건부로 이어붙어 켜진 조합에 따라 줄바꿈 위치가 매번 달라 "제멋대로"로
  보였음(보리 지적). 타이틀 옆 = 고정노선연동 전용 자리, 타이틀 아래 줄 =
  나머지(즐겨찾기·수수료·파렛트·담당자) 고정 자리로 구조 분리. 상세는
  `docs/report.md`, 아직 승인 전.

### 7. 미수금/정산 관리 `[x]` 완료 (2026-09-15)
- **탭이 그리드가 아니라 분리된 토글이었던 것 수정**[발견·수정
  2026-09-15, 보리 스크린샷 지적] — 원본 전용 `.receivable-tabs`(단일
  컨테이너, 그리드 2열)를 이식, 다른 화면(정비/주유/기타) 탭 컴포넌트
  재사용을 걷어냄.
- **안내 문구 삭제**[2026-09-15, 보리 결정] — react-app에만 있던
  "운행 일지 세부 입력에서 자동으로 모읍니다..." 삭제.
- **버튼 문구** — "미수금 상세"(원본과 동일)/"입금완료"(원본 "입금 완료
  처리"와 다름 — 보리가 고른 축약 표현, 의도적 차이).
- **버튼 위치**[수정 2026-09-15] — 미수금 금액 아래 구분줄, 하단 가로
  버튼줄로 원본과 동일하게 이동. 근본 원인은 단순 위치가 아니라
  `management-list-card`(다른 화면 공용, 가로분할) 레이아웃을 그대로
  가져다 써서 카드 골격 자체가 달랐던 것 — 원본 `.receivable-group-card`
  세로 스택 구조로 교체.
- **액션 버튼 글자가 세로로 한 글자씩 깨지던 것 수정**[발견·수정
  2026-09-15] — "상세"/"입금 완료" 텍스트 버튼에 다른 화면 SVG 아이콘
  버튼 전용 클래스(`action-icon-btn`, 34×34px 고정)를 써서 생긴 버그.
  원본 전용 `.receivable-detail-btn`/`.receivable-complete-btn`(자동
  폭 텍스트 버튼) 이식.
- **원본은 메인 차량을 파란색 네모 칩으로 표시** — react-app은 평문
  텍스트였던 것을 `management-badge car-type` 배지로 수정.
- `[x]` 완료 — 금액 뒤 "원" 앞 띄어쓰기 — react-app "100,000 원"(띄움)
  vs 원본 "100,000원"(붙임, 단 원본도 같은 카드 안에서 공백 유무가
  뒤섞여 있어 원본과 완전히 맞추는 건 불가능이라 **공백 없이 통일**
  (보리 결정)). `formatWon()`(`domain/money.js`)이 리포트·정비주유
  기타·세금계산서 등 14개 파일이 공유하는 전역 함수라 처음엔 미수금
  화면 범위 밖으로 미뤘지만, 이후 별도로 앱 전체 범위로 수정
  완료(react-app `241d4db`+`86d805e`, 보리 브라우저 실검증·최종
  승인 2026-09-15).
- 구현: react-app `12a9dbb`(`receivables.css` 신규 + 목록/카드/상세
  원본 `.receivable-*` 구조 이식). CI 초록·보리 브라우저 실검증·최종
  승인. 착수지시서·구현기록은 `ubiquitous-parakeet` 커밋
  `c75c7b6`/`aed68cd` 참고.

### 8. 정비/주유/기타 — 전부 `[x]` 완료 (2026-09-15)
- `[x]` 완료 — **"정비/주유/기타" 탭 위치가 반대** — react-app은 탭
  3개가 화면 맨 위(타이틀 바로 아래)에 있고, 원본은 탭이 목록 맨 아래
  ("+추가" 버튼 다음)에 있던 것 → 탭·월합계·"+추가"를 화면 하단 고정
  dock 하나로 이관(react-app `eda52e4`).
- `[x]` 완료 — **월 합계 카드 위치·문구** — "N건" 항목 개수 표시 제거,
  레이블을 "{월}월 {kindLabel}"로 변경, 하단 dock 안으로 이동
  (react-app `eda52e4`).
- `[x]` 완료 — **항목 분류칩(예: "엔진오일")에 SVG 아이콘 누락**[보리
  스크린샷 지적 2026-09-15] — 원본은 🔧 아이콘 + 텍스트, react-app은
  텍스트만이던 것 → kind별(정비/주유/기타) SVG 아이콘 복원, 원본
  path와 문자 단위 대조 완료(react-app `16ff413`).
- `[x]` 완료 — **결제수단·분류 라벨이 칩(pill) 아니라 민무늬 텍스트**
  [보리 스크린샷 지적 2026-09-15] — 원본은 결제수단만 칩이고 분류는
  민무늬인 비대칭이지만, "발견한 비대칭은 앱 통일 우선"
  원칙(위 §0 참고, §6 수수료 칩 통일 사례와 동일 기준)에 따라 원본
  그대로 포팅하지 않고 결제수단·분류 등 전체를 동일 칩 스타일로 통일
  (react-app `8d9e06a`/`bb359b4`). 상세 조사·착수지시서는
  `docs/report.md` §8-A·§8-B, 보리 브라우저 실검증·push·CI 초록·AI
  코드 리뷰 전부 완료.
- `[x]` 조치 불필요(정정, 2026-09-14) — ~~react-app 항목마다
  "수정삭제" 버튼아이콘이 바로 보임(차량관리·거래처와 같은 패턴),
  원본엔 없음~~ **틀렸음, 원본도 동일하게 아이콘 버튼 있음**
  (`maint-fuel-misc.js:504`, `editDetailSvg`/`deleteDetailSvg`).
  §2-1-B가 이미 원본과 동일하게 아이콘 이식(react-app `489e9fd`).
- 날짜별 항목 내용(엔진/카드/엔진·미션, 금액)은 동일.

### 9. 세금계산서 `[x]` 완료(2026-09-16) — `6ee827f`/`03e1e86`/`eba8eb1`/`e55c044`/`2ac7e73`/`3e22169`, 보리 브라우저 실검증으로 원본과 외관 일치 확인

> 2026-09-16 보리 스크린샷 대조(매출 발행 탭, 작성 전, 라이트/모바일).
> 상단 회사명 헤더(원본 스크린샷에서 빨간색으로 가린 부분)는 이미
> 이관된 공용 컴포넌트로 확인됨 — 대조 대상 아님, 그대로 사용.

- **"공급가" vs "공급가액" 레이블 다름** — react-app "공급가", 원본
  "공급가액"(더 정확한 세무 용어).
- **합계 줄에 라벨 없음** — react-app 카드는 "공급가 350,000원 / 세액
  35,000원 / 385,000원"(세 번째 숫자에 "합계"라는 라벨이 없이 숫자만),
  원본은 "공급가액/세액/**합계** 385,000원"으로 라벨이 붙어 있음.
- **카드별 "작성 전" 상태 뱃지 안 보임** — 원본은 개별 거래처 카드에도
  "작성 전" 상태가 표시되는데, react-app 카드엔 상단 요약("작성 전 1 /
  발급 완료 0")만 있고 카드 자체엔 상태 표시가 안 보임.
- **상단 탭/토글 숫자 뱃지 스타일 차이**(2026-09-16 구체화) — 원본은
  탭("매출 발행"/"기사 매입"/"수수료 발행")과 토글("작성 전"/"발급
  완료")의 숫자가 별도 회색 동그란 뱃지(pill)로 분리 표시, react-app은
  숫자가 텍스트 옆에 그냥 붙어있음(뱃지 스타일 없음).
- **안내박스 제목 누락**(2026-09-16 신규) — 원본은 안내문 위에
  "회사 사업자 정보가 필요합니다."라는 굵은 제목 줄이 있는데, react-app엔
  이 제목 줄 자체가 없고 바로 본문 설명 문장("마이페이지 → 개인정보에서…")
  으로 시작함.
- **금액 박스 스타일 차이**(2026-09-16 신규) — 원본은 카드 안
  공급가액/세액을 회색 박스로 감싸 나란히 표시, react-app은 박스 없이
  텍스트만 나열.
- **버그: 카드 액션 버튼 텍스트 세로로 깨짐**(2026-09-16 신규,
  원인 확인) — 원본은 "작성하기"/"엑셀 저장"/"발급 완료" 3개 버튼이
  가로로 정상 배치되는데, react-app은 버튼이 안 보이고 그 자리에 글자가
  세로로 한 글자씩 쌓여 보임. 원인:
  `src/components/TaxInvoiceEntryList.jsx:46-54`의 세 버튼이 전부
  아이콘 전용으로 설계된 `.action-icon-btn` 클래스를 재사용
  (`src/side-menu.css:184-198`, 다른 화면에선 `width:34px;height:34px`
  정사각형 아이콘 버튼으로만 씀) — `white-space: nowrap`·
  `flex-shrink:0`이 없어 공백 없는 한글 텍스트가 글자 단위로 줄바꿈되며
  좁은 flex 행 안에서 최소너비까지 눌려 깨짐. 단순 스타일 통일이 아니라
  실제 렌더링 버그로 분류.

#### 슬라이스 1·2 이후 후속 재작업 대기 항목(2026-09-16 보리 스크린샷 재확인)

- **활성 탭/토글의 숫자 뱃지 색이 원본과 다름** — 탭 배경 자체는 이미
  같은 토큰(`var(--primary-color)`, `side-menu.css:608`
  `.toggle-btn.active-work`와 `receivables.css:30`
  `.receivable-tab.active`가 동일)이라 통일돼 있지만, 슬라이스 1이 만든
  `.tab-count-badge`(`tax-invoice.css:64`)가 활성/비활성 상태 구분 없이
  항상 `background: var(--input-bg); color: var(--sub-text-color)`
  고정이라 파란 활성 탭 위에서 흐릿하게 보임. 원본은 활성 탭 위 숫자가
  밝게 대비됨(정확한 원본 색상값은 미확인, 후속 슬라이스 착수 시 확인
  필요). "매출 발행/기사 매입/수수료 발행" 탭과 "작성 전/발급 완료"
  토글 둘 다 해당.
- **"작성 전"/"발급 완료" 토글을 §7(미수금) 스타일로 통일**(보리 지시,
  2026-09-16) — 현재 §9는 §8(정비/주유/기타)과 공유하는
  `.settings-segmented-control.maint-fuel-tabs`+`.toggle-btn` 조합을
  쓰는데, 보리가 원하는 모양은 §7 `ReceivablesListPage.jsx`의
  `.receivable-tabs`/`.receivable-tab`(그리드 2열, 둥근 바깥 테두리 +
  안쪽 필 버튼, 활성 시 `var(--primary-color)` 채움 — `receivables.css:4-34`)
  스타일. `.receivable-tab`은 현재 `.receivables-page` 스코프 + §7
  전용(다른 화면 미사용, `grep -rn "receivable-tab" src` 결과 2파일뿐)이라
  §9에서 그대로 재사용하려면 공유 클래스로 승격하거나 §9 전용 동일
  스타일 클래스를 새로 만들어야 함 — 둘 다 §7 무영향, 방식은 다음
  착수지시서에서 결정.
- **`<년 월>` 날짜 이동 + 탭이 카드로 안 묶여있음**(보리 지적,
  2026-09-16) — §3 매출 화면(`OwnerRevenueView.jsx:110`/
  `DriverRevenueView.jsx:58`)은 `<년 월>` 날짜 네비게이터와 "매출
  발행/기사 매입/수수료 발행" 탭 자리에 해당하는 부분을 `revenue-top-card`
  하나로 감싸서(`revenue.css:98-105`: 배경·테두리·radius 24px·그림자
  있는 카드) 한 덩어리 카드로 보여주는데, §9는 `TaxInvoiceToolbar.jsx`가
  `<div className="maint-fuel-nav">`(날짜 네비게이터)와
  `<div className="settings-segmented-control maint-fuel-tabs">`(탭)를
  Fragment 안에 그냥 나열만 해서 카드 배경 없이 따로 떠 있음. §9도
  `revenue-top-card`와 동일한 카드로 묶어야 함 — 재사용 방식(공유 클래스
  승격 vs §9 전용 동일 스타일)은 다음 착수지시서에서 결정.
- **안내 제목이 박스 밖으로 빠져나와 있음**(보리 지적, 2026-09-16,
  슬라이스 1에서 만든 것 자체의 미비점) — "회사 사업자 정보가
  필요합니다." 제목과 "마이페이지 → 개인정보에서…" 설명 문장이 원본은
  하나의 안내 박스 안에 제목(굵게)+설명 두 줄로 같이 들어있는데,
  react-app은 `TaxInvoicePage.jsx:152-159`에서 제목(`<p className="tax-invoice-guide-title">`)과
  설명(`<p className="tax-invoice-guide">`)을 서로 다른 형제 `<p>`로
  분리해서 렌더링 — `tax-invoice-guide-title`엔 배경·테두리가 없어서
  (`tax-invoice.css:37-43`) 박스 스타일(`tax-invoice-guide`,
  `tax-invoice.css:45-52`)이 있는 설명 문장 위에 제목만 박스 밖에 붕
  떠 보임. 두 줄을 같은 박스 안에 합쳐야 함(제목+설명을 하나의 wrapper
  안에 넣고 그 wrapper에 `tax-invoice-guide`의 박스 스타일을 적용).
- **버그: 요약 카드와 목록 카드가 붙어 있음**(보리 지적, 2026-09-16,
  원인 확인) — 공용 `.summary-card`(`shared-controls.css:68-76`)에
  `margin-bottom`이 없고, `TaxInvoicePage.jsx:172-182`에서
  `<div className="summary-card">`(월간 정산 요약) 바로 뒤에
  `<TaxInvoiceEntryList>`(카드 목록)가 이어져서 둘 사이 여백을 주는
  요소가 없어 붙어 보임. `.management-list-card`는 자기 아래쪽에만
  `margin-bottom: 14px`이 있어서(위 카드와의 간격은 못 만듦) 첫 카드
  앞엔 여백이 전혀 없는 상태. `.summary-card`는 다른 화면 3곳
  (`CalendarMonthSummary.jsx`/`ReportDetailView.jsx`/
  `ReportSummaryContent.jsx`)도 같이 쓰는 공용 클래스라 그 규칙 자체는
  안 건드리고, §9 쪽에 여백을 추가하는 방식으로 고칠 예정.
- 그 외 카드 전체 레이아웃(간격·정렬 등)이 원본과 전체적으로 달라
  보인다는 지적(2026-09-16) — 항목별로 말로 정리하기 어려운 수준이라,
  후속 슬라이스 착수 시 두 화면을 나란히 놓고 항목별로 다시 짚을 예정.

### 10. 운송비 내역서 (요약) `[ ]`
- **CSS 분리는 이미 완료됨**(`components/report/report.css`, `cf54899`,
  2026-09-15 — 위 상단 "완료 화면 + CSS 분리 현황"·"`side-menu.css` 잔여
  경계 지도" G 참고). 아래는 아직 남은 UI 내용 대조 항목.
- 메인 차량 리포트 본문의 날짜별 표(9월1일 운행, 금액 등) — 날짜·운행
  횟수·금액이 중앙 표시가 아님.
- 세부내역서·PDF 다운로드·이미지 저장·공유가 원본은 아이콘 표시.
- **원본과 다르게 수정할 필요 있는 부분**:
  - 세부내역서·PDF 다운로드·이미지 저장·공유 아이콘을 정산 요약 카드
    하단으로 배치.
  - "2026년 9월 운송비 내역서" 글자 제거(년/월 칩은 유지).
  - 계좌번호랑 예금주 자리 변경.

### 11. 개인정보 `[ ]`
- **상단 안내 문구 내용이 다름** — react-app "입력하면 바로 저장됩니다.
  소속 연결은 나중에 붙입니다.", 원본 "입력한 내용은 운송비 내역서와 정산
  정보에 사용됩니다." — 같은 자리인데 서로 다른 내용을 안내함.
- 01~04 섹션 앞의 라벨과 숫자가 너무 벌어져 있음(예: "사업자 정보"와
  내역서에 표시되는 업체 정보 힌트 문구 사이 거리가 너무 벌어짐).
- 04 계정 아래 힌트 문구가 "로그인 상태"로만 되어 있으나 원본은 "로그인
  상태와 계정 연결 관리"로 되어 있음.
- "테스트 계정으로 사용 중입니다." 문구가 있어서 제거 필요.
- 원본은 회원탈퇴가 버튼이 아니라 밑줄 친 작은 글자 "회원탈퇴"로 되어 있음.

### 12. 기사연동관리 (기존 §11 실측 상세) `[ ]`
- **목록 섹션 타이틀 없음** — 원본은 "기사 연결 현황 / 연동 상태와 차량
  할당 기간을 관리합니다."라는 섹션 설명이 있는데 react-app엔 없음.
- **버튼 문구와 버튼 형태 전부 다름** — react-app "수정/연동 완료/삭제",
  원본 "연결 상태 확인/초대 수정/초대 취소".
- **항목 표시 형식 차이** — react-app은 "11가1111 · 2026-09-07 · 종료일
  없음"처럼 한 줄로 압축. 원본은 "할당 차량/할당 기간/할당 중" 상태뱃지까지
  라벨을 붙여 여러 줄로 표시.
- **버그(§9와 동일 원인, 2026-09-16 발견)** — `DriverConnectionPage.jsx:131-139`
  "기록 조회" 버튼도 아이콘 전용 `.action-icon-btn` 클래스를 텍스트 버튼에
  재사용 중 — §9 코드 조사 중 같은 클래스(`action-icon-btn`)를 쓰는 다른
  화면을 확인하다 발견. §9 슬라이스에서 `.action-icon-btn` 자체는 안 건드리고
  텍스트 전용 변형 클래스(`action-text-btn`, `side-menu.css:224-231`)를
  새로 추가하는 방식으로 고칠 예정이라 이 화면엔 영향 없음 — §12 차례에
  같은 클래스로 교체.
  **주의(2026-09-16 추가)**: §9가 이후 카드를 세로 스택으로 재구성하면서
  `TaxInvoiceEntryList.jsx`는 `action-text-btn` 대신 §9 전용
  `tax-invoice-action-btn`으로 갈아탐 — 그 결과 `action-text-btn`은 현재
  JSX 소비처가 0곳(`grep -rn "action-text-btn" src` 결과 CSS 정의뿐).
  **죽은 코드로 오인해 지우지 말 것** — §12 차례에 쓸 용도로 남겨둔 것.

### 13. 앱 설정 `[ ]`
- 테마 선택의 다크모드 칩 앞에 달 아이콘이 없음(원본은 라이트모드일 땐
  해 아이콘으로 바뀜).
- 달력 일일 표시 방식에서 횟수/금액이 그리드 방식이 아님 — 원본은 그리드.
- **앱 통일성(원본 이관 아님, 2026-09-16 보리 지시, 미착수)** — 달력
  일일 표시 방식의 "횟수/금액" 버튼을 정비내역추가(§8)의 "결제 방식"
  버튼([ExpenseFormModal.jsx:142](react-app/src/components/ExpenseFormModal.jsx:142)
  `.segment-control`/`.segment-btn`, `expense-form.css:31-56` — 너비
  140px 고정폭·8px 라운드 테두리·활성 시 원색 채움)과 같은 스타일로
  통일. 현재 "횟수/금액"은 `settings-segmented-control`+`.toggle-btn`
  조합(위 "공용 버그" 24px 알약 이슈까지 겹쳐 있음)이라 모양이 아예
  다름. 지금 처리 안 함 — 착수 시 착수지시서 별도 작성.
- (게스트모드) 데이터 관리가 제일 하단에 위치해 있고 상세 UI도 다름.
- 기사차량 운행일지 설정이 원본의 앱 설정엔 없음(원본 기준으로 **불필요
  — 제거 대상**).

### 14. 고객센터 `[ ]`
- "자주 묻는 질문" 문구가 왼쪽으로 쏠려 있음 — 중앙으로.

### (보리 직접 대조예정) 미확인한 화면 
미연동 서브 차량화면,연동서브 차량 관리화면

## 진행 상태

(2026-09-08 참고 기록): 차주 + 라이트 + 모바일 = 13개 화면 중 12개
완료(로그인 화면 제외). 다크 테마·게스트 계정·로그인 화면은 1차에서
못 봄 — 2차에서 게스트/다크 언급 없는 항목은 아직 미확인일 수 있음.
